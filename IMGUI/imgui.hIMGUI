#pragma once
#include "imconfig.h"
#include <float.h>
#include <stdarg.h>
#include <stddef.h>
#include <string.h>

#define IMGUI_VERSION "1.89.4"

namespace ImGui {
    struct ImGuiContext;
    ImGuiContext* CreateContext(void* shared_font_atlas = NULL);
    void DestroyContext(ImGuiContext* ctx = NULL);
    void NewFrame();
    void Render();
    void Begin(const char* name, bool* p_open = NULL, int flags = 0);
    void End();
    void Text(const char* fmt, ...);
    bool Button(const char* label, const ImVec2& size = {0,0});
    bool Checkbox(const char* label, bool* v);
}
